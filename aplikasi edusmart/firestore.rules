rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Izinkan baca tulis oleh siapa pun (tidak ada batas waktu)
      allow read, write: if true;
    }
  }
}
